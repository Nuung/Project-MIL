import BaseScene from "./BaseScene";

class StoryDesciptionScene extends BaseScene{
    constructor(key){
        super(key);
        //storyline variable?
    }

    init(data){
        // Get description storyData data
    }
    
    preload(){
        //preload the images of the chapter
    }

    create(){
        //1. Add story text display
        //2. Add story skipping method.
        //3. Get next sentence or paragraph-line
    }
}

export default StoryDesciptionScene;